# Milestone 0 — Pi 400 Measurement Report

> **STATUS: PENDING.** The numbers below must be filled in by running the
> exported arm64 binary on the reference Raspberry Pi 400, following the
> protocol in [docs/milestone-0-brief.md §7](../milestone-0-brief.md). CI can
> prove the project builds and exports; only the physical Pi 400 can produce
> the performance answer. See the **Decision point** at the bottom.

## Environment

| Thing | Value |
|---|---|
| Date | _<fill in>_ |
| Reference machine | Raspberry Pi 400 (BCM2711, VideoCore VI @ 500 MHz, 4 GB) |
| OS | Raspberry Pi OS (Debian 13 trixie) arm64 — _<version>_ |
| Godot version | `4.7.2-stable` (from `.godot-version`) |
| Commit hash | _<`git rev-parse HEAD`>_ |
| Renderer | `opengl3` (Compatibility), launched with `--rendering-driver opengl3` |
| Binary | `build/linux-arm64/HenGrenade.arm64` (CI artifact, exported) |
| Display | wired HDMI to a TV/monitor; desktop at the resolution under test |

## Protocol

1. Pi 400, mains power, wired display, nothing else running, desktop at the resolution under test.
2. Launch the **exported** arm64 binary (never a debug build or the editor):
   `./HenGrenade.arm64 --rendering-driver opengl3`
3. Enter the stress scene (F2). Vsync **off** (press V). 60-second run.
4. Record average / p99 frame time and draw calls from the on-screen metrics overlay.
5. Repeat at 720p, 1080p, and 4K output (cycle with R).
6. Repeat with vsync **on** to confirm a stable, hitch-free 60 over 60 s.
7. Record CPU temperature at start and end (`vcgencmd measure_temp`).

## Results — frame time (60 s run, vsync off)

| Output | Avg frame (ms) | p99 frame (ms) | Draw calls | Pass? |
|---|---|---|---|---|
| 720p  | _<fill>_ | _<fill>_ | _<fill>_ | _<p99 ≤ 16.6 ms?>_ |
| 1080p | _<fill>_ | _<fill>_ | _<fill>_ | _<p99 ≤ 16.6 ms?>_ |
| 4K    | _<fill>_ | _<fill>_ | _<fill>_ | _informational>_ |

## Results — vsync on (60 s run)

| Output | Steady 60 FPS? | Visible hitching? | Notes |
|---|---|---|---|
| 720p  | _<fill>_ | _<fill>_ | |
| 1080p | _<fill>_ | _<fill>_ | |
| 4K    | _<fill>_ | _<fill>_ | |

## Thermal

| | Start (°C) | End (°C) | Throttled? |
|---|---|---|---|
| CPU temp | _<fill>_ | _<fill>_ | _<`vcgencmd get_throttled`>_ |

## Hardware pass — input

| Check | Result |
|---|---|
| Four F310s via powered USB hub — all four squares move independently | _<pass/fail + notes>_ |
| Three pads + built-in keyboard — four players, no hub | _<pass/fail + notes>_ |
| Hot-plug: unplug and replug a bound pad rebinds to its slot | _<pass/fail + notes>_ |
| Four identical pads disambiguated despite matching GUIDs | _<pass/fail + notes>_ |

## Exports

| Artifact | Runs on target? | Notes |
|---|---|---|
| Windows x86_64 (`HenGrenade.exe`) | _<pass/fail>_ | |
| Linux arm64 (`HenGrenade.arm64`) | _<pass/fail>_ | |
| arm64 texture loading — no `No loader found for resource` errors | _<pass/fail>_ | |

## Decision point

- **If 720p vsync-off p99 frame time ≤ 16.6 ms AND draw calls ≤ 20** → M0 passes.
  Proceed to M1.
- **If the frame criterion fails** → bisect with the stress scene toggles
  (particles / explosions / HUD) first. If a single feature dominates, tighten
  the budget. If the baseline itself is too slow, that is the ADR 0001 trigger:
  switch to LÖVE before writing any gameplay.

**M0 verdict:** _<PASS / FAIL — bisect / FAIL — ADR trigger>_

---

## Appendix: re-run history

Every later milestone re-runs the stress scene and appends a row here so
performance regressions are visible as they happen.

| Milestone | Commit | 720p p99 (ms) | Draw calls | Notes |
|---|---|---|---|---|
| M0 | _<hash>_ | _<value>_ | _<value>_ | baseline |
